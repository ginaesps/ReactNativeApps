import {StyleSheet} from 'react-native';

export const colors = {
  primary: '#282c84',
  white: '#e20c79',
};

export const baseStyles = StyleSheet.create({
  Title: {
    color: 'white',
    fontWeight: 'bold',
    fontSize: 25,
    letterSpacing: 5,
  },
});
